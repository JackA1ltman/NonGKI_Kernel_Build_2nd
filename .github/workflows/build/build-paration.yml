name: Common Build Preparation
on:
  workflow_call:

jobs:
  preparation:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: |
          echo "hello"
          mkdir kernel_test
